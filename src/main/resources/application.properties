spring.application.name=connectify
server.baseUrl=${BASE_URL:http://localhost:8081}
server.port=${PORT:8081}

#mysql database configurations
#datasource url 
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:connectlydb}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:root}

#jpa configurations
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

#security configurations
# spring.security.user.name=nidhi
# spring.security.user.password=nidhi

# Oauth2 client configurations
# Google configurations

# GitHub configurations

#Cloudinary configurations

#email configurations

##########
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}
spring.profiles.active=${ACTIVE_PROFILE:dev}

#logging.level.org.springframework=DEBUG
